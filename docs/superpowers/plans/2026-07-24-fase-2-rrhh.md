# Fase 2 — Migrar RRHH Implementation Plan

**Goal:** Portar `gestion-operarios` a `app/(app)/rrhh/`, con su esquema reconciliado
contra el núcleo, el motor de cálculo migrado a Supabase (preservando sus 54 tests),
todas las pantallas, y los datos reales de producción. Ver el spec de diseño:
`docs/superpowers/specs/2026-07-24-fase-2-rrhh-design.md`.

**Convenciones:** mismas que Fases 0-1. Commits pequeños en español, directo a `main`,
`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Checkboxes = tracker de
progreso entre sesiones — esta fase es grande, se espera que abarque varias sesiones.

**Fuente:** repo original clonado en `/Users/delfina/Downloads/APPRRHH` (solo lectura,
para copiar/portar código). Reporte de exploración completo (esquema Prisma, motor de
cálculo, rutas, tests, páginas) volcado en la sesión que lo generó — resumen clave ya
está en el spec de diseño.

---

### Task 1: Esquema — reconciliación de núcleo + tablas de dominio RRHH

**Files:** `supabase/migrations/008_rrhh_schema.sql`, `009_rrhh_rls.sql`,
`010_seed_rrhh.sql`

- [x] **Step 1:** Enums: `origen_fichada`, `tipo_dia`, `tipo_ausencia` (12 valores),
      `estado_franco`, `tipo_liquidacion`, `estado_liquidacion`, `tipo_feriado`.
- [x] **Step 2:** Tabla de extensión `rrhh_empleados_datos` (sindicato, fecha
      nacimiento, género, escala de vacaciones override).
- [x] **Step 3:** Tablas de dominio: `jornadas`, `fichadas`, `rrhh_import_batches`,
      `rrhh_import_staging`, `feriados`, `config_liquidacion` (singleton, seed con
      valores default), `calculos_diarios`, `ausencias`, `vacaciones`, `francos`,
      `liquidaciones`.
- [x] **Step 4:** Funciones `puede_editar_rrhh()`, `es_admin_rrhh()`,
      `tiene_acceso_rrhh()` + RLS de todas las tablas nuevas + policies adicionales en
      `empleados`/`sectores` (núcleo) para admins de RRHH.
- [x] **Step 5:** Seed de prueba: catálogo de 3 jornadas de ejemplo (Mañana/Tarde/
      Noche). Sectores/empleados reales llegan con la migración de datos (Task 14).
- [x] **Step 6:** Verificar tipos: `npx tsc --noEmit` — sin errores.
- [x] **Step 7:** Commit. **Pendiente aplicar** contra la base real: armé
      `supabase/migrations_fase2_combined.sql` (008-010) para que se corra en el SQL
      Editor, mismo procedimiento que con Mantenimiento.

### Task 2: Motor de cálculo — funciones puras + 54 tests portados

**Files:** `lib/rrhh/engine/calculo.ts` (+`.test.ts`), `lib/rrhh/engine/vacaciones.ts`,
`lib/rrhh/engine/recalcular-puro.ts` (+`.test.ts` — `ajustarFichadasPorTurno`,
`intervalsParaDia`, `detectarTurno`, `anclaTurno`), `lib/rrhh/dates.ts`,
`lib/rrhh/excelImport.ts` (+`.test.ts` — parsers), `lib/rrhh/reconciliarMarcaciones.ts`
(+`.test.ts`), `lib/rrhh/constants.ts`

- [x] **Step 1:** Portar `calculo.ts` + sus 11 tests, literal.
- [x] **Step 2:** Portar `vacaciones.ts` (`diasCorrespondientes`).
- [x] **Step 3:** Portar `dates.ts` completo, literal.
- [x] **Step 4:** Portar las funciones puras de `recalcular.ts` a
      `lib/rrhh/engine/recalcular-puro.ts` (`ajustarFichadasPorTurno`,
      `intervalsParaDia`, `detectarTurno`, `anclaTurno`) + sus 13 tests.
- [x] **Step 5:** Portar parsers de `excelImport.ts` (`parseDateString`,
      `parseMarcaciones`, `parseNumeroAR`, y todo lo demás del archivo — resultó ser un
      solo archivo en el original, no dos) + sus 10 tests.
- [x] **Step 6:** Portar `reconciliarMarcaciones` (vive en `excelImport.ts` en el
      original) + sus 14 tests, en `lib/rrhh/reconciliarMarcaciones.test.ts`.
- [x] **Step 7:** Portar `constants.ts` (`SECTORES_LUNES_A_VIERNES`).
- [x] **Step 8:** `npm test` → 54/54 en verde (48 tests portados del motor/parsers +
      los 6 ya existentes del núcleo de la Fase 0).
- [x] **Step 9:** Commit.

### Task 3: Motor de cálculo — recálculo contra Supabase (I/O)

**Files:** `lib/rrhh/engine/recalcular.ts`, `lib/rrhh/config.ts`

- [x] **Step 1:** `recalcularEmpleadoPeriodo(supabase, empleadoId, desde, hasta)` —
      misma lógica que el original, reescrita contra `@supabase/supabase-js`.
- [x] **Step 2:** `recalcularSectorPeriodo(supabase, sectorId | null, desde, hasta)`.
- [x] **Step 3:** `getConfigLiquidacion(supabase)`.
- [x] **Step 4:** Verificar tipos y tests — 54/54 en verde.
- [x] **Step 5:** Commit.

### Task 4: Nav, layout y guarda de acceso del módulo

**Files:** `lib/mantenimiento/auth.ts` → generalizar o crear `lib/core/module-auth.ts`
(el patrón `nivelXxxDe`/`puedeEditarXxx` se repite para RRHH — evaluar si conviene
parametrizarlo por módulo en vez de duplicar), `app/(app)/rrhh/layout.tsx`,
`lib/rrhh/context.tsx`, `lib/core/nav.ts` (ya tiene las entradas de RRHH desde la Fase
0 — revisar si siguen vigentes: Empleados, Asistencia, Vacaciones y francos,
Liquidaciones + Remises anidado).

- [x] **Step 1:** Se duplicó el patrón de Mantenimiento tal cual (`lib/rrhh/auth.ts`:
      `nivelRrhhDe`/`puedeEditarRrhh`/`esAdminRrhh`) — más rápido y de menor riesgo que
      generalizar en medio de una fase grande; queda anotado como refactor posible.
- [x] **Step 2:** Layout del módulo (`app/(app)/rrhh/layout.tsx`) con guarda de acceso +
      `NivelRrhhProvider` + `ConfirmProvider`.
- [x] **Step 3:** `NAV` ampliado: Dashboard, Empleados, Fichadas, Asistencia,
      Ausencias, Vacaciones, Francos, Liquidaciones, Analítico, Administración RRHH.
- [x] **Step 4:** Verificar tipos — sin errores.
- [x] **Step 5:** Commit.

### Task 5: Empleados (listado, ficha, import Excel con staging)

**Files:** `app/(app)/rrhh/empleados/**`, `app/(app)/rrhh/empleados/[id]/**`,
`app/api/rrhh/empleados/**`, `app/api/rrhh/empleados/import/**`

- [x] **Step 1:** Listado + alta manual.
- [x] **Step 2:** Import masivo desde Excel — preview (parsea y guarda en
      `rrhh_import_staging` con token) → confirmar (lee el staging por token, resuelve/
      crea Empresa y Sector transversal por nombre, upsert por legajo).
- [x] **Step 3:** Ficha de empleado completa (datos + edición, 4 pestañas: fichadas con
      corrección/validación de horas extra vía `FichadaEditModal`, ausencias con
      clasificación de faltas sin fichada, vacaciones con balance, francos), baja segura
      con conteo de historial. De paso quedó armada toda la API de asistencia/ausencias/
      vacaciones/francos/fichadas que reutilizan las Tasks 7-10.
- [x] **Step 4:** Verificar tipos y build — 34 rutas compilando, 54/54 tests en verde.
- [x] **Step 5:** Commit.

Migración de RLS adicional: `011_rrhh_rls_fix.sql` (calculos_diarios pasa a
`puede_editar_rrhh()` en vez de `es_admin_rrhh()`, porque el recálculo automático es
efecto secundario de acciones de nivel edición, no solo admin — igual que el original).

### Task 6: Fichadas (listado, alta/edición manual, import desde reloj biométrico)

**Files:** `app/(app)/rrhh/fichadas/**`, `app/api/rrhh/fichadas/**`,
`app/api/rrhh/fichadas/import/**`

- [x] **Step 1:** Listado + alta/edición/borrado manual (dispara recálculo) — API ya
      armada en la Task 5, se sumó la página.
- [x] **Step 2:** Import desde Excel (formato combinado "Marcaciones" y separado),
      usando `reconciliarMarcaciones`, reemplazo de fichadas `IMPORTADO` previas del
      mismo (empleado, día), deduplicación, staging vía `rrhh_import_staging`. Corregido
      al portar: el import de fichadas en el original **no** era admin-only (a
      diferencia de empleados) — se usa `puede_editar_check`, no `es_admin_check`.
- [x] **Step 3:** Export a Excel — de paso se agregaron también los de ausencias,
      vacaciones y francos (mismo helper `xlsxResponse`).
- [x] **Step 4:** Verificar tipos y build — 42 rutas compilando.
- [x] **Step 5:** Commit.

### Task 7: Asistencia (por período, por día, faltas sin clasificar)

**Files:** `app/(app)/rrhh/asistencia/**`, `app/api/rrhh/asistencia/**`

- [x] **Step 1:** Resumen por período + faltas sin clasificar.
- [x] **Step 2:** Vista por día (roster) — se unificaron ambas vistas en una sola
      página (`/rrhh/asistencia`) con pestañas Período/Día, en vez de dos rutas
      separadas como el original, para simplificar el nav. Se movió
      `FichadaEditModal` a `components/rrhh/` (compartido entre ficha de empleado y
      asistencia por día).
- [x] **Step 3:** Validar horas extra + marcar horas manuales — API ya armada en la
      Task 5.
- [x] **Step 4:** Commit.

### Task 8: Ausencias / Licencias

**Files:** `app/(app)/rrhh/ausencias/**`, `app/api/rrhh/ausencias/**`

- [x] **Step 1:** CRUD ya armado en la Task 5 (API) + export Excel (Task 6). La página
      de listado global (`/rrhh/ausencias`) unifica "Ausencias" (injustificadas) y
      "Licencias" (justificadas, sin vacaciones) en pestañas, igual que se hizo con
      Asistencia — el original las tenía como dos rutas separadas.
- [x] **Step 2:** Commit.

### Task 9: Vacaciones

**Files:** `app/(app)/rrhh/vacaciones/**`, `app/api/rrhh/vacaciones/**`

- [x] **Step 1:** Balance por empleado/año (`diasCorrespondientes` + escala) — API ya
      armada en la Task 5.
- [x] **Step 2:** Historial global + CRUD de períodos tomados. Una sola página
      `/rrhh/vacaciones` con pestañas Balance/Historial (mismo criterio que Asistencia
      y Ausencias — el original tenía 2 rutas).
- [x] **Step 3:** Commit.

### Task 10: Francos

**Files:** `app/(app)/rrhh/francos/**`, `app/api/rrhh/francos/**`

- [x] **Step 1:** Listado + filtro por estado + cambio PENDIENTE→TOMADO + export.
- [x] **Step 2:** Commit.

### Task 11: Liquidaciones

**Files:** `app/(app)/rrhh/liquidaciones/**`, `app/api/rrhh/liquidaciones/**`

- [ ] **Step 1:** Generación (recalcula período, separa horas extra validadas/sin
      validar, aplica fórmulas de montos).
- [ ] **Step 2:** Listado, cierre, borrado, export por empleado y planilla completa.
- [ ] **Step 3:** Commit.

### Task 12: Dashboard + Analítico

**Files:** `app/(app)/rrhh/page.tsx` (o `dashboard/`), `app/(app)/rrhh/analitico/**`,
`app/api/rrhh/dashboard/**`, `app/api/rrhh/analitico/**`

- [ ] **Step 1:** Dashboard (KPIs del día, gráficos por sector, top ausencias/tardanzas).
- [ ] **Step 2:** Analítico personal (ausentismo, tardanza, distribución por género/
      antigüedad/empresa).
- [ ] **Step 3:** Cache de recálculo (adaptar `recalcCache.ts` a un entorno serverless —
      evaluar `unstable_cache`/revalidation de Next.js en vez de `Map` en memoria).
- [ ] **Step 4:** Commit.

### Task 13: Administración RRHH (jornadas, feriados, configuración)

**Files:** `app/(app)/rrhh/administracion/**`, `app/api/rrhh/jornadas/**`,
`app/api/rrhh/feriados/**`, `app/api/rrhh/configuracion/**`

- [ ] **Step 1:** CRUD de jornadas (catálogo de turnos).
- [ ] **Step 2:** CRUD de feriados.
- [ ] **Step 3:** Configuración de reglas de cálculo (horas normales, multiplicadores,
      escala de vacaciones, corte de sábado, feriado como domingo).
- [ ] **Step 4:** Commit.

### Task 14: Migración de datos reales de producción

**Completa.** `DATABASE_URL` de Neon (producción de gestion-operarios) conseguido y
usado solo en modo lectura.

- [x] **Step 1:** Auditados los 17 sectores reales contra el seed de prueba de
      Mantenimiento: 4 coincidían de nombre (Calidad/Producción/Mantenimiento/
      Administración, sembrados por Mantenimiento como no-transversales, por empresa).
      Decisión del usuario: mantenerlos separados — los de RRHH se crean transversales
      con sufijo " (RRHH)" para desambiguar en la UI.
- [x] **Step 2:** Script de migración (Node + `pg` para leer Neon, `@supabase/supabase-js`
      con `service_role` para escribir el SdG — nunca se escribió/borró nada en el
      origen). Corrido una vez, con guarda de seguridad (aborta si `fichadas` ya tiene
      filas). Resultado: 2 empresas reconciliadas, 17 sectores nuevos transversales, 4
      usuarios reales reconciliados (3 cuentas nuevas en Supabase Auth + 1 ya existente
      como `admin_sistema` desde la Fase 1) — se excluyeron a propósito las 2 cuentas de
      prueba del README original (`admin@empresa.com`, `encargado@empresa.com`), 70
      empleados + datos extendidos, 6 jornadas reales (reemplazando el catálogo de
      ejemplo sembrado en la 010), 16 feriados, `config_liquidacion` actualizada con los
      valores reales (la escala de vacaciones real difiere del default: tope 25 años, no
      999), 11 lotes de importación, 1149 fichadas, 12178 cálculos diarios, 31
      ausencias, 52 francos. `VacationPeriod`/`PayrollPeriod` no se migraron por estar
      vacíos en origen (0 filas).
- [x] **Step 3:** Verificación de integridad: conteos vía REST contra el SdG coinciden
      exactamente con los del origen en todas las tablas; muestreo de un empleado
      confirma empresa/sector correctamente reconciliados.
- [x] **Step 4:** El script vivió en el scratchpad de la sesión (no en el repo) y se
      borró junto con la credencial de Neon al terminar — no hace falta volver a
      correrlo. Las 3 contraseñas temporales de las cuentas nuevas se compartieron por
      chat; pendiente que cada persona las cambie.

### Task 15: Verificación final de la Fase 2

- [ ] **Step 1:** `npm test` → verde (54 tests portados + los ya existentes de
      Fases 0-1).
- [ ] **Step 2:** `npx tsc --noEmit` → sin errores.
- [ ] **Step 3:** `npm run build` → build exitoso.
- [ ] **Step 4:** Flujo manual completo con datos reales migrados: login → Dashboard →
      Empleados → Fichadas (import) → Asistencia → validar horas extra → generar y
      cerrar una liquidación → Vacaciones → Francos → Administración.

**Definición de "hecho" para Fase 2:** RRHH funciona de punta a punta sobre el núcleo
unificado, con los datos reales de producción migrados, el motor de cálculo
preservando su comportamiento (validado por los 54 tests originales), y sin ninguna
tabla propia de la app vieja (`User`, `Employee`, etc. no existen en el SdG).

## Notas para la fase siguiente

- **Fase 3 (Remises):** `empleados` ya tiene `domicilio` (pensado para esto) y ahora
  además `rrhh_empleados_datos` — revisar si Remises necesita más campos de la ficha
  de empleado antes de decidir si extiende `rrhh_empleados_datos` o crea su propia
  extensión.
- **Permisos por sector diferidos**: cuando se definan los roles/permisos específicos
  mencionados por el usuario, revisar si conviene un esquema genérico (aplicable a
  Mantenimiento y Remises también) en vez de uno propio de RRHH.
